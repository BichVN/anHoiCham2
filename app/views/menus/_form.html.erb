aanhancut1<%= form_for @menu,:html => { :multipart => true }  do |f| %>
  <% if @menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu.errors.count, "error") %> prohibited this menu from being saved:</h2>

      <ul>
      <% @menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :menuName %><br>
    <%= f.text_field :menuName %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  
  <div class="field"> attachment 
    <%= f.file_field :attach %>
  </div>

  <div class="field">  Picture 
    <%= f.file_field :pic %>
  </div>

  <%= f.label :imgs %>
  <%= f.file_field :imgs, multiple: true, accept: "image/jpeg, image/jpg, image/gif, image/png" %>


<%= f.fields_for :foods do |form_food| %> 
  <% if @food.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food.errors.count, "error") %> prohibited this food from being saved:</h2>

      <ul>
      <% @food.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
aanhancut2

  <div class="field">
    <%= form_food.label :food_name %><br>
    <%= form_food.text_field :name %>
  </div>

<%= form_food.fields_for :ingredients do |form_ingredient| %>
  <% if @ingredient.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ingredient.errors.count, "error") %> prohibited this ingredient from being saved:</h2>

      <ul>
      <% @ingredient.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form_ingredient.label :ingredient_name %><br>
    <%= form_ingredient.text_field :name %>
  </div>
  <div class="field">
    <%= form_ingredient.label :quality %><br>
    <%= form_ingredient.text_field :quality %>
  </div>
  <% end %>
  <% end %>

  <div class="actions">
    <%= f.submit "Create menu"%>
  </div>
<% end %>
