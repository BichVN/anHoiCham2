<p id="notice"><%= notice %></p>
<table>
  <thead>
    <tr>
      <th>Typeofmenu</th>
      <th>Content</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= menu.typeOfMenu %></td>
        <td><%= menu.content %></td>
        <td><%= menu.user %></td>
        <td><%= link_to 'Show', menu %></td>
        <td><%= link_to 'Edit', edit_menu_path(menu) %></td>
        <td><%= link_to 'Destroy', menu, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
  </tbody>
</table>

<br>

