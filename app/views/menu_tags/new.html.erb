
<h1>Menu Tags</h1>
<% @tags.each do |tag| %>
	  <ul>
	      <li><%= link_to tag.tag, chosetag_path(@menu,tag) %></li>	
	  </ul>
<% end %>
<h1>New Menu Tag</h1>

<%= render 'form' %>

<%= link_to 'Back', root_path %>
